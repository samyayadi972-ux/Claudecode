# Base de données PostgreSQL (Neon en production, PostgreSQL local en dev)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/client_crm?sslmode=require"

# NextAuth — générer avec: openssl rand -base64 32
NEXTAUTH_SECRET="REMPLACER_PAR_UN_SECRET_FORT"
NEXTAUTH_URL="https://votre-domaine.vercel.app"
